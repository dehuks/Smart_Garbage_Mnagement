package com.smart.garbage;  // Add this package statement at the top

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class RequestPickupActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_request_pickup);

        // Additional setup for pickup request form can go here
    }
}
